// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // 站点最终的公开地址(上线后改成你的真实域名即可)
  // site: 'https://你的域名',
  trailingSlash: 'ignore',
});
